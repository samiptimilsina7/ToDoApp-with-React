export const Test=(props)=>{
    return(
    <div>
        {/* {fruits}<br/>
        {fruits.map((fruit)=>
            <div>{fruit}</div>
        )} */}
        this is a {props.title} component
    </div>
    );
}